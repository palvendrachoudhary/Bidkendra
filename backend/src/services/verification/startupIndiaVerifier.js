const { CheckStatus, Method, createResult } = require('./checkStatus'); exports.verify = async (extractedText, apiConfig) => { if (extractedText.toLowerCase().includes('startup india') || extractedText.toLowerCase().includes('dipp') || extractedText.toLowerCase().includes('dpiit recognized')) { return createResult({ checkId: 'startup', checkName: 'Startup India', checkStatus: CheckStatus.NEEDS_MANUAL_REVIEW, method: Method.DOCUMENT_SCAN, source: 'Document Content', message: 'Startup India keywords found. Manual verification required on DPIIT portal (CAPTCHA).' }); } else { return createResult({ checkId: 'startup', checkName: 'Startup India', checkStatus: CheckStatus.NOT_CONFIGURED, method: Method.NONE, source: 'Document Content', message: 'Not claiming Startup India exemption.' }); } };