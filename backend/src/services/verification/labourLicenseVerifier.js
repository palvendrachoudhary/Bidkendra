const { CheckStatus, Method, createResult } = require('./checkStatus'); exports.verify = async (extractedText, apiConfig) => { if (extractedText.toLowerCase().includes('labour license') || extractedText.toLowerCase().includes('clra') || extractedText.toLowerCase().includes('contract labour')) { return createResult({ checkId: 'labourLicense', checkName: 'Labour License', checkStatus: CheckStatus.NEEDS_MANUAL_REVIEW, method: Method.DOCUMENT_SCAN, source: 'Document Content', message: 'CLRA Labour License keywords found. Manual review required.' }); } else { return createResult({ checkId: 'labourLicense', checkName: 'Labour License', checkStatus: CheckStatus.FAILED, method: Method.DOCUMENT_SCAN, source: 'Document Content', message: 'No CLRA Labour License declared.' }); } };