const { CheckStatus, Method, createResult } = require('./checkStatus'); exports.verify = async (extractedText, apiConfig) => { if (extractedText.toLowerCase().includes('nsic') || extractedText.toLowerCase().includes('single point registration')) { return createResult({ checkId: 'nsic', checkName: 'NSIC Status', checkStatus: CheckStatus.NEEDS_MANUAL_REVIEW, method: Method.DOCUMENT_SCAN, source: 'Document Content', message: 'NSIC SPRS claimed. Manual verification required.' }); } else { return createResult({ checkId: 'nsic', checkName: 'NSIC Status', checkStatus: CheckStatus.NOT_CONFIGURED, method: Method.NONE, source: 'Document Content', message: 'No NSIC registration claimed.' }); } };